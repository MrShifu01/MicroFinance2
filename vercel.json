{
    "buildCommand": "cd client && npm install && npm build",
    "outputDirectory": "client/dist",
    "framework": "react",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/server.js"
        }
    ]
}